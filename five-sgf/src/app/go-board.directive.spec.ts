import { GoBoardDirective } from './go-board.directive';

describe('TenukiBoardDirective', () => {
  it('should create an instance', () => {
    const directive = new GoBoardDirective();
    expect(directive).toBeTruthy();
  });
});
